# Models

Model binaries are runtime assets, not source files.

Expected local checkpoint for music-to-molecule inference:

- `pinao8/音乐到分子/model_last(2).pt`

The checkpoint is intentionally ignored by Git. Keep model download URLs, checksums, and placement instructions here instead of committing `.pt` files.
