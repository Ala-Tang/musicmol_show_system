flask>=2.0
flask-cors>=3.0
mido>=1.3
# 合一模式 (MUSICMOL_UNIFIED=1) 内嵌 painojs RDKit FastAPI，需与 server/rdkit_embed 一致：
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# 音乐→分子（/api/music_to_molecule*）：需 torch、selfies；推理实现见 `../音乐到分子/inference.py`，权重见该目录 readme.md
torch>=2.0
selfies>=2.1.0
# RDKit 为 Flask 分子校验所需；本机若已有 conda/pip 安装的 rdkit 可忽略下一行
# Windows 可尝试：pip install rdkit
# 或：conda install -c conda-forge rdkit
