# Ops

Target home for deployment and operations assets.

Current production entry points remain in place:

- `../run_unified.sh`
- `/mnt/PianoMol/setup_proxy_and_restart.sh`
- Nginx config documented in `../README.md`

Files move here only after the production path has a wrapper at the old location.
